============
Installation
============

At the command line::

    $ easy_install dbaas_credentials

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dbaas_credentials
    $ pip install dbaas_credentials